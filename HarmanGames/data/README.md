# Game data (optional)

- **Trackzy:** The app reads artist data from `../Trackzy/DataCollection/output/artists_raw.json` by default. For a self-contained deploy (e.g. Render), copy that file here as `artists_raw.json`.
- **SportsGuesser:** Served from the `../SportsGuesser` project; no data files needed here.
